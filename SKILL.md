---
name: cambly-review
description: |
  对 Cambly / 外教 1v1 课程的 transcript Markdown 做结构化 review,
  在同目录下产出 `<原文件名>-review.md`。基于 ob-secure 五规则
  (目录 → 概念总览 → 八面受敌 → Obsidian 美观 → 勘误 → Agent 总结)
  格式,内容覆盖 5 维:重点词汇拓展(释义/同义辨析/用法/例句)、
  外教教的思维方法论、面试/课堂注意事项、所有表达不严谨之处、
  可直接复用的句式模板。

  **文件夹增量模式**:用户通常会传一个上层大文件夹(里面是一堆子文件夹,
  每个子文件夹是一节课),skill 会递归扫一层子文件夹,
  **跳过那些已经生成过 `-review.md` 的子文件夹**,
  只处理还没 review 过的。如果用户传单个 md 文件,按单文件模式走。

  Trigger when user: 提供 cambly/外教 1v1 transcript md 并说 "review"、
  "梳理"、"整理"、"/ob-secure + 课程分析"、"cambly-review";或粘贴
  转写文字要求按五规则分析。Triggers include both English and Chinese:
  "review my Cambly lesson", "帮我把这节课重点词汇整理出来",
  "外教教的思考方法总结一下", "指出我表达里不严谨的地方",
  "把这堆课都过一遍" / "review 整个文件夹"。

  Do NOT use for: 通用 markdown 编辑、翻译、初学者词汇表、
  TOEFL/IELTS 标准化备考(用 dedicated 备考 skill)、课堂外聊天记录、
  短对话(无结构化教学内容)。
---

# cambly-review

> 本 skill 的全部产出都遵循 [[ob-secure]] 五规则。如果你不熟悉那个 skill,先加载它,再开始执行本 skill。

## Inputs to collect

调用本 skill 前必须先确认两件事:

1. **目标文件路径** —— 用户给出具体 md 文件,或一个文件夹路径。
   - **单文件模式**:用户给的是 `xxx.md` 路径 → 直接处理这一份。
   - **文件夹模式**:用户给的是一个文件夹路径。判断是哪种:
     - 子文件夹里直接放 md(平铺结构)→ 在这个文件夹内扫所有 cambly 风格 md。
     - **外层大文件夹(默认增量模式)** → 文件夹下面是一堆子文件夹,每个子文件夹是一节课(常见形态:`<外教名>-<日期>-<N> minutes/<同前>.md`)。**每个子文件夹独立判断是否已有 `-review.md`;已有的整个跳过,只处理没生成过 review 的子文件夹**。
   - 判断"cambly 风格":文件含 `Lesson ID` / `外教` / `课程总结 · AI 反馈` / `语音转文字` 四个标志性段落之一即可认定为候选。
   - 文件夹模式下,子文件夹内只有"已存在 `-review.md`"才跳过;其他子文件夹(含空文件夹、含非 cambly 风格 md)都按"待处理"列出。
2. **产出文件路径** —— 默认 = 原文件同目录、文件名追加 `-review` 后缀。例如 `Schalk-June 26th.md` → `Schalk-June 26th-review.md`。

如果用户没说"是否所有文件都做",先列清单让他确认,不要批量静默执行。**但增量模式下不要反复确认**:用户传大文件夹的默认意图就是"扫一下,把没做过的做了"。直接列"已跳过 X / 待处理 Y",让用户看完决定继续或调整即可。

## Procedure

按 6 个阶段顺序执行,前一阶段没产出就不进入下一阶段(避免在空 transcript 上硬编内容)。

### 阶段 0 · 扫描与过滤(文件夹模式必做,单文件模式跳过)

文件夹模式下,进入正式 review 之前先做两件事:

1. **扫描**:用 `ls` / `glob` 列出目标文件夹下的直接子文件夹(不递归到子文件夹内部),每个子文件夹内部再找 cambly 风格 md。
2. **过滤**:对每个子文件夹,检查同目录内是否已存在 `<同名前缀>-review.md`(或任意 `-review.md`,取决于子文件夹结构):
   - **已存在 → 整个子文件夹跳过**,记到"已跳过"清单。
   - **不存在 → 加入"待处理"清单**。
3. **汇报**:开始批量之前,先在回复里列:
   - 已跳过(子文件夹名 + 已有 review 文件名)
   - 待处理(子文件夹名 + 内部 transcript md)
   - 异常(子文件夹里没有 cambly 风格 md 的、空的)
4. **询问 vs 直接执行**:
   - 增量模式下,默认直接处理"待处理"清单,不要每节课都问。
   - 只有当"待处理"数量 ≥ 3 时,先快速列清单给用户确认一句"按这个清单做,行吗?",避免他临时改主意;1-2 节直接做,不用问。

### 阶段 1 · 解析原文档结构(必做)

把 md 切成 5 个固定段落,缺哪段就在 review 里跳过该段:

| 原段落 | 用途 |
|--------|------|
| Lesson 元信息(外教名 / 日期 / 时长 / WPM / Unique words) | review 顶部的 frontmatter + 概念总览 |
| 课程总结 · AI 反馈(Cambly 自带 Grammar/Vocabulary 纠错) | review 的"AI 已标注"小节直接复用 |
| 语音转文字(Transcript) | 主体材料,从中抽取词汇/方法论/纠错 |
| 课堂聊天(Chat) | 外教用 chat 补充的关键解释 |
| 课件(Slides) | 通常为空,空就跳过 |

### 阶段 2 · 抽取 5 维内容(主体工作)

对 transcript 做 5 轮扫描,每轮抓一类信息。顺序不能乱 —— 因为后面的轮次依赖前面的标签。

| 维度 | 扫描什么 | 输出形式 |
|------|---------|---------|
| **重点词汇** | 外教特别强调的词、重复读的词、课后 chat 发的生词 | 表格:词 / 释义 / 词性 / 用法 / 例句。词汇 ≥ 5 个时**额外加同义辨析小节** |
| **思维方法论** | 外教反复出现的结构、模板、比喻、口诀 | 一节一段,每段先引外教原话 + 拆解公式 |
| **注意事项** | 外教明确说"don't..." 的雷区 + 文化敏感话题 + 时态/介词硬伤 | Callout `[!danger]` / `[!warning]` / `[!tip]` |
| **表达不严谨** | Cambly AI 已标注 + 外教当场纠正 + 自查发现(ASR 转写误差、信噪比低的对答) | 分 3 个子小节,自查发现必须明确标注"外教没纠正" |
| **可复用句式** | 外教给的完整句子、你练过的范例 | 直接给可复制的模板,标"面试/日常/商务"用途 |

> [!warning] 5 维都重要,但**词汇和方法论是骨架**。即使时间和精力受限,这两维不能省。表达纠错可以短一点,但自查发现一定要列 —— 这是用户最看重的部分。

### 阶段 3 · 应用 ob-secure 五规则格式化

把阶段 2 的内容按下面顺序组装:

```
1. frontmatter (date / tutor / duration / topic / related_note)
2. ## 目录        ← 用 Obsidian 锚点 #章节标题
3. ## 概念总览     ← 5 行表格,维度 ↔ 核心要点 ↔ 预期效果
4. ## 一、重点词汇拓展
5. ## 二、思维方法论
6. ## 三、注意事项
7. ## 四、表达不严谨之处
8. ## 五、可复用句式 / 模板
9. ## 勘误与版本记录  ← 用 ~~删除线~~ 标记对原笔记的修正
10. ## Agent 可读总结  ← SKILL / DOMAIN / TRIGGER_WHEN / STANCE + 优先级表 + 决策点 + 局限
```

obs-secure 风格的格式细节(锚点链接、Callout 类型、表格三列、同义辨析表格)严格按 [[ob-secure]]。

### 阶段 4 · 自查清单(发布前必过)

发布前对照下面 5 条自查 —— 任何一条不满足就不输出:

1. [ ] 5 维都有内容吗?(重点词汇 / 方法论 / 注意事项 / 纠错 / 句式)
2. [ ] 概念总览表覆盖了所有章节吗?(每章节至少一行)
3. [ ] 表达纠错区分了 3 个来源吗?(Cambly AI / 外教纠正 / 自查)
4. [ ] 删除线勘误只在末尾的"勘误与版本记录"小节出现吗?(正文不删除任何内容)
5. [ ] Agent 总结有信令、优先级、决策点、局限 4 个要素吗?

### 阶段 5 · 输出与通知

把生成的 review 文件路径告诉用户,并在回复里附 `<media src="..." />` 标签(用绝对路径)—— 用户的文件系统不可直接访问,必须靠 media tag 传递文件。

如果一次生成了多个 review(文件夹批量模式),用 `<media />` 列出每一个,不要漏。

## Output contract

### 文件命名

`<原文件名>-review.md`,放在**原文件同目录**。

### 必含结构

- frontmatter(5 个字段:`date` / `tutor` / `duration` / `date_of_lesson` / `topic`)
- 目录 + 概念总览(两个表格)
- 5 维内容(顺序:词汇 → 方法论 → 注意事项 → 表达纠错 → 句式)
- 勘误(用 `~~删除线~~` 标记,正文不动)
- Agent 总结(代码块信令 + 优先级表 + 决策点 + 局限)

### 行数预期

单节课 review 通常 250-450 行。低于 150 行说明内容不够,高于 600 行说明冗余 —— 自查后调整。

### 风格

- 中文为主(用户的工作语言),英文例句、英文模板原文保留
- Callout 必用(每节至少 1 个 `[!warning]` 或 `[!tip]`)
- 表格优于段落(同义辨析、词汇列表、注意事项一律表格化)

## Failure handling

| 失败 | 怎么处理 |
|------|---------|
| transcript ASR 噪声太大,大量"~"或断句错误 | 标注"以下为推测,原 transcript 不清晰",不要硬猜外教原意 |
| Cambly AI 反馈为空 | 改用外教当场纠正 + 自查发现作为纠错主体 |
| 词汇太少(< 3 个) | 跳过"同义辨析"小节,但保留词汇表;在 Agent 总结里注明"本节课偏自由聊天,词汇密度低" |
| 用户给出文件夹路径但部分文件不是 cambly 风格 | 只处理含 `Lesson ID` 字段的文件,其他跳过并在最终回复里列"跳过的文件"清单 |
| transcript 是中文(用户写中文笔记而不是 cambly 导出) | 不属于本 skill 范围,提示用户"本 skill 处理 cambly 导出格式,中文笔记建议用通用 markdown review 流程" |
| 同文件夹已有同名 `-review.md` | **默认跳过,不覆盖**(对应文件夹增量模式);在 final reply 的"已跳过"清单里列出来。**如果用户明确说"重做/重新 review/redo"**,才覆盖,并在 final reply 顶部红字提示"⚠️ 覆盖了 X 文件,原内容已丢失" |
| 6 节批量时总行数过多 | 每个 review 独立发媒体标签,**不要合并成一个文件**;在 final reply 顶部加一句"按时间倒序排列,6 份独立 review" |
| 增量模式下所有子文件夹都已有 review | 报"已全部 review 过,无需处理",并按"按时间倒序"列出已有 review 的清单,问用户"是否要全部重做?" |
| 增量模式下"待处理"清单为空但有非 cambly 文件 | 提示"找到 X 个非 cambly 风格的 md,跳过",但不打扰用户(在 final reply 末尾的"异常"清单里写一行) |

## Examples

### Example 1 · 单文件 review

**Input:**
> 用户给出 `/Users/llazuli/Documents/CamblyNotes/Dennis D-July 1st, 2026-60 minutes/Dennis D-July 1st, 2026-60 minutes.md`,说"给我 review 一下这节课"。

**Action:**
1. 解析 → 识别为 cambly 风格(有 Lesson ID / 课程总结 / Transcript / Chat)。
2. 抽取 → 词汇:laid off / fired / KPI / overcommit / burnout / perfectionism / streak / delegation 等;方法论:"你 = 销售员"、"弱点翻优势"、"5 年规划答法";注意事项:别提钱、别提 fired、要反问;纠错:AI 标注 2 条 + 外教纠正若干 + 自查 ~10 条;句式:60 秒自我介绍 + 5 类高频面试题。
3. 格式化 → 按 ob-secure 五规则。
4. 自查 → 5 条全过。
5. 输出 → `<media src=".../Dennis D-July 1st, 2026-60 minutes-review.md" />`

### Example 2 · 文件夹增量 review(默认模式)

**Input:**
> 用户给出 `/Users/llazuli/Documents/CamblyNotes/`(外层大文件夹,下面 6 个外教子文件夹,
> 其中 2 个子文件夹里已经有 `*-review.md` 了),说"把这堆课都过一遍"。

**Action:**
1. 扫描阶段 0 → 列出 6 个子文件夹;检查每个子文件夹是否已有 `-review.md`:
   - 已跳过(2 个):`Schalk-June 5th, 2026-30 minutes/`(已有 `...-review.md`)、`Dennis D-June 12th, 2026-60 minutes/`(已有)
   - 待处理(4 个):`Schalk-June 26th, 2026-30 minutes/`、`Dennis D-July 1st, 2026-60 minutes/`、`Mia-June 18th, 2026-30 minutes/`、`Tom-July 3rd, 2026-45 minutes/`
2. 快速跟用户确认一句:"4 份待处理,2 份已跳过。直接做这 4 份?"
3. 用户点头 → 并行处理 4 份,每份独立 review,不合并。
4. 输出 → 4 个 `<media />` 标签,每个独立;并在 final reply 顶部列"已跳过 2 份,本次新增 4 份"。

### Example 2b · 用户明确要求重做(覆盖模式)

**Input:**
> 用户说"那份 Schalk June 5th 的 review 重做一下,我改主意了"。

**Action:**
1. 检测到 `-review.md` 已存在,但用户明确说"重做" → 走覆盖模式。
2. Final reply 顶部红字提示:`⚠️ 覆盖了 Schalk-June 5th, 2026-30 minutes-review.md,原内容已丢失`。
3. 正常产出新 review。
4. 同样 `<media />` 输出。

### Example 3 · 反例(不该触发)

**Input:**
> 用户说"帮我翻译一下这份 markdown"。

**Action:**
> 不调用本 skill。翻译任务跟外教课 review 没有重叠,使用通用翻译 skill 或直接处理。

## Anti-patterns(避免)

1. **不要把 5 维内容塞进 1 个大段落** —— 必须每维独立小节,带 Callout 和表格。
2. **不要删除 transcript 原内容** —— 只在末尾"勘误"小节用删除线标记。
3. **不要外教没纠正的就不写** —— 自查发现必须列,这是用户最看重的内容。
4. **不要把 ASR 转写误差当成语法错** —— 标注"推测"或跳过。
5. **不要硬凑词汇凑方法论** —— 自由聊天课词汇少就少,Agent 总结里说明即可。
6. **不要套 ob-secure 的同时把它的目录结构原样复制** —— 本 skill 是 ob-secure 的"领域实例化",不是复读 ob-secure 模板。
7. **不要在没有 transcript 的情况下硬编** —— 标记"无 transcript,跳过 5 维",只产出元信息和空 review 占位。
8. **不要默默覆盖已存在的 review** —— 文件夹增量模式下,默认是"跳过"不是"覆盖"。用户没说"重做"就不要碰 `-review.md`;要覆盖时,final reply 顶部必须红字提示。

## Related

- [[ob-secure]] —— 格式骨架,本 skill 的所有产出遵循它的五规则
- [[skill-creator]] —— 本 skill 的创建方法论
- Cambly 课程文件命名约定:`<外教名>-<M月 Dst, YYYY>-<N> minutes/<同前>.md`